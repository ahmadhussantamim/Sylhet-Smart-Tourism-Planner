// pages/ComingSoon.jsx
// -----------------------------------------------------------------------
// Temporary placeholder for pages that later Parts will build
// (dashboards, destinations list, guides list, etc.). Keeping these
// routes wired up now means navigation links don't 404 while we build
// the project part by part.
// -----------------------------------------------------------------------

export default function ComingSoon({ title }) {
  return (
    <div className="container" style={{ padding: '80px 24px', textAlign: 'center' }}>
      <h1>{title}</h1>
      <p></p>
    </div>
  );
}
